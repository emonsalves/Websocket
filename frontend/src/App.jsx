import io from 'socket.io-client'
import { useState } from 'react'

// const socket = io('http://localhost:3000')
const socket = io('/')

const App = () => {
  return (
    <div>App</div>
  )
}
export default App
